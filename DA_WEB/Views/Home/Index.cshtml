@model IEnumerable<DA_WEB.Models.Product>
@{
    ViewData["Title"] = "Kizuna — Thời Trang Tối Giản Nhật Bản";
    Layout = "~/Views/Shared/_LayoutKizuna.cshtml";
}

<!-- Custom cursor -->
<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- ======= HEADER ======= -->
<header id="mainHeader">
    <a href="/" class="logo">
        <svg class="logo-mark" viewBox="0 0 48 48" fill="none">
            <path d="M24 4C25.7818 14.2173 33.7827 22.2182 44 24C33.7827 25.7818 25.7818 33.7827 24 44C22.2182 33.7827 14.2173 25.7818 4 24C14.2173 22.2182 22.2182 14.2173 24 4Z" fill="currentColor" />
        </svg>
        <span class="logo-text">Kizuna</span>
    </a>
    <nav>
        <a href="/Shop">New Arrivals</a>
        <a href="/Shop">Collections</a>
        <a href="@Url.Action("Philosophy", "Home")">Philosophy</a>
        <a href="@Url.Action("About", "Home")">About</a>
    </nav>
    <div class="header-actions">
        <button class="icon-btn">Search</button>
        <a href="/Cart/Index" class="icon-btn" style="text-decoration:none">Bag <span class="cart-count">0</span></a>

        @if (User.Identity != null && User.Identity.IsAuthenticated)
        {
            <span class="icon-btn">@User.Identity.Name</span>

            @if (User.IsInRole(DA_WEB.Models.SD.Role_Admin))
            {
                <a asp-area="Admin"
                       asp-controller="Dashboard"
                       asp-action="Index"
                       class="icon-btn"
                       style="color: var(--accent-gold)">
                    Admin
                </a>
            }

            <form asp-area="Identity"
                  asp-page="/Account/Logout"
                  asp-route-returnUrl="/"
                  method="post"
                  style="display:inline">
                <button type="submit" class="icon-btn">Logout</button>
            </form>
        }
        else
        {
            <a asp-area="Identity" asp-page="/Account/Login" class="icon-btn">Sign In</a>
            <a asp-area="Identity" asp-page="/Account/Register" class="icon-btn">Register</a>
        }
    </div>
</header>

<!-- ======= HERO ======= -->
<section class="hero">
    <div class="hero-left">
        <div class="hero-bg-img"></div>
        <p class="hero-eyebrow">SS25 Collection</p>
        <h1 class="hero-title">
            Sự Giao Thoa<br />
            Giữa <em>Truyền Thống</em><br />
            Và Hiện Đại
        </h1>
        <p class="hero-subtitle">Khám phá vẻ đẹp tối giản mang đậm hơi thở Nhật Bản qua từng đường kim mũi chỉ.</p>
        <div class="hero-cta-row">
            <a href="/Shop" class="btn-primary">
                Khám Phá Ngay
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M5 12h14M12 5l7 7-7 7" />
                </svg>
            </a>
            <a href="#" class="btn-ghost">Xem Lookbook</a>
        </div>
    </div>
    <div class="hero-right">
        <div class="hero-right-img"></div>
        <div class="hero-tag">New</div>
    </div>
    <div class="hero-scroll-hint">
        <div class="scroll-line"></div>
        <span>Scroll</span>
    </div>
</section>

<!-- ======= MARQUEE ======= -->
<div class="marquee-wrap">
    <div class="marquee-track">
        <span class="marquee-item">
            <span>Premium Linen</span><span class="marquee-dot"></span>
            <span>Artisan Crafted</span><span class="marquee-dot"></span>
            <span>Wabi-Sabi Philosophy</span><span class="marquee-dot"></span>
            <span>Sustainable Materials</span><span class="marquee-dot"></span>
            <span>Minimalist Cuts</span><span class="marquee-dot"></span>
            <span>Japanese Aesthetic</span><span class="marquee-dot"></span>
        </span>
        <span class="marquee-item">
            <span>Premium Linen</span><span class="marquee-dot"></span>
            <span>Artisan Crafted</span><span class="marquee-dot"></span>
            <span>Wabi-Sabi Philosophy</span><span class="marquee-dot"></span>
            <span>Sustainable Materials</span><span class="marquee-dot"></span>
            <span>Minimalist Cuts</span><span class="marquee-dot"></span>
            <span>Japanese Aesthetic</span><span class="marquee-dot"></span>
        </span>
    </div>
</div>

<!-- ======= NEW COLLECTION ======= -->
<section class="section-collection">
    <div class="section-header reveal">
        <div>
            <div class="section-label">Shop the look</div>
            <h2 class="section-title">
                New Collection<br />
                <span style="font-family:'Cormorant Garamond',serif;font-style:italic;font-weight:300;color:var(--muted);font-size:0.68em">
                    Spring — Summer 2025
                </span>
            </h2>
        </div>
        <a href="/Shop" class="view-all">
            View all
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M5 12h14M12 5l7 7-7 7" />
            </svg>
        </a>
    </div>

    <div class="products-grid">
        @{
            int delayIndex = 0;
        }
        
        @foreach (var item in Model)
        {
            // Tạo hiệu ứng load dần đều (delay) cực mượt như code gốc của bạn
            string delayClass = delayIndex > 0 ? $"reveal-delay-{delayIndex}" : "";
            string defaultImg = "https://via.placeholder.com/400x500?text=Kizuna+Product"; // Ảnh tạm nếu lỡ quên up ảnh

            <a href="/Shop/Detail/@item.Id" style="text-decoration:none;color:inherit;display:block">
                <div class="product-card reveal @delayClass">
                    <div class="product-img-wrap">
                        <div class="product-img" style="background-image:url('@(string.IsNullOrEmpty(item.ImageUrl) ? defaultImg : item.ImageUrl)')"></div>
                        
                        @if (delayIndex == 0)
                        {
                            <div class="product-badge">New</div>
                        }
                        
                        <div class="product-quick-add">+ Quick Add</div>
                    </div>
                    <h3 class="product-name">@item.Name</h3>
                    <div class="product-meta">
                        <span class="product-price">@item.Price.ToString("N0")đ</span>
                        <div class="product-color-swatches">
                            <span class="swatch" style="background:#e8ddd0"></span>
                            <span class="swatch" style="background:#2a2520"></span>
                        </div>
                    </div>
                </div>
            </a>
            
            delayIndex++;
        }
    </div>
</section>

<!-- ======= PHILOSOPHY ======= -->
<section class="section-philosophy">
    <div class="philosophy-img">
        <div class="philosophy-img-inner"></div>
        <div class="philosophy-number">侘</div>
    </div>
    <div class="philosophy-content reveal">
        <div class="section-label" style="color:var(--accent-gold)">
            <span style="display:block;width:24px;height:1px;background:var(--accent-gold)"></span>
            Our Philosophy
        </div>
        <h2 class="philosophy-title" style="margin-top:16px">
            Triết Lý<br /><em>Wabi-sabi</em>
        </h2>
        <p class="philosophy-text">
            Wabi-sabi là nghệ thuật tìm kiếm vẻ đẹp trong sự không hoàn hảo và sự giản đơn thuần khiết.
            Tại Kizuna, mỗi sản phẩm được tạo ra không chỉ để mặc, mà là một sự tôn vinh đối với sự vô thường.
        </p>
        <div class="pillar-divider"></div>
        <div class="philosophy-pillars">
            <div>
                <div class="pillar-title">Sự Tĩnh Lặng</div>
                <div class="pillar-text">Thiết kế mang lại cảm giác bình yên và tự do cho người mặc.</div>
            </div>
            <div>
                <div class="pillar-title">Sự Bền Vững</div>
                <div class="pillar-text">Chất lượng trường tồn qua nhiều mùa mốt và thời gian.</div>
            </div>
            <div>
                <div class="pillar-title">Thủ Công</div>
                <div class="pillar-text">Kỹ thuật truyền thống kết hợp quy trình sản xuất hiện đại.</div>
            </div>
            <div>
                <div class="pillar-title">Tự Nhiên</div>
                <div class="pillar-text">Linen hữu cơ, cotton tự nhiên và lụa tơ tằm thuần chất.</div>
            </div>
        </div>
        <a href="/Shop" class="btn-primary" style="align-self:flex-start">
            Khám Phá Thêm
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" style="width:14px">
                <path d="M5 12h14M12 5l7 7-7 7" />
            </svg>
        </a>
    </div>
</section>

<!-- ======= FEATURED ======= -->
<section class="section-featured">
    <div style="text-align:center;margin-bottom:64px" class="reveal">
        <div class="section-label" style="justify-content:center">Featured Selection</div>
        <h2 class="section-title" style="margin-top:12px">Được Yêu Thích Nhất</h2>
    </div>
    <div class="featured-grid">
        @{
            var featuredList = ViewBag.FeaturedProducts as List<DA_WEB.Models.Product>;
            int fIndex = 0;
        }
        
        @if (featuredList != null)
        {
            @foreach (var item in featuredList)
            {
                string delayClass = fIndex > 0 ? $"reveal-delay-{fIndex}" : "";
                string imgUrl = string.IsNullOrEmpty(item.ImageUrl) ? "https://via.placeholder.com/600x800" : item.ImageUrl;

                <a href="/Shop/Detail/@item.Id" style="text-decoration:none;display:block;position:relative">
                    <div class="featured-card reveal @delayClass">
                        <div class="featured-img" style="background-image:url('@imgUrl')"></div>
                        
                        @if (fIndex == 1)
                        {
                            <div class="featured-center-badge">Editor's Pick</div>
                        }
                        
                        <div class="featured-overlay">
                            <div class="featured-label">Nổi Bật</div>
                            <div class="featured-name">@item.Name</div>
                            <div class="featured-desc">@item.Price.ToString("N0")₫</div>
                        </div>
                    </div>
                </a>
                fIndex++;
            }
        }
    </div>
</section>

<!-- ======= NEWSLETTER ======= -->
<section class="section-newsletter">
    <div class="newsletter-bg"></div>
    <div class="newsletter-content reveal">
        <div class="newsletter-label">Stay Connected</div>
        <h2 class="newsletter-title">Kết Nối<br />Với <em>Chúng Tôi</em></h2>
        <p class="newsletter-subtitle">
            Nhận thông tin về bộ sưu tập mới và các ưu đãi đặc quyền sớm nhất.<br />
            Chúng tôi không gửi spam — chỉ những điều thực sự quan trọng.
        </p>
        <div class="newsletter-form">
            <input class="newsletter-input" type="email" placeholder="Email của bạn" />
            <a href="/Identity/Account/Login" class="newsletter-btn" style="text-decoration: none; display: inline-flex; align-items: center; justify-content: center;">Đăng Ký</a>
        </div>
    </div>
</section>

<!-- ======= FOOTER ======= -->
<footer>
    <div class="footer-top">
        <div>
            <div class="footer-brand-logo">
                <svg width="18" height="18" viewBox="0 0 48 48" fill="none">
                    <path d="M24 4C25.7818 14.2173 33.7827 22.2182 44 24C33.7827 25.7818 25.7818 33.7827 24 44C22.2182 33.7827 14.2173 25.7818 4 24C14.2173 22.2182 22.2182 14.2173 24 4Z" fill="#bd002c" />
                </svg>
                Kizuna
            </div>
            <p class="footer-brand-desc">
                Thời trang tối giản lấy cảm hứng từ tinh thần Nhật Bản.
                Mang lại sự tĩnh lặng và tự do trong từng trang phục.
            </p>
            <div class="footer-socials">
                <a href="@Url.Action("Contact", "Home")" class="footer-social">IG</a>
                <a href="@Url.Action("Contact", "Home")" class="footer-social">FB</a>
                <a href="@Url.Action("Contact", "Home")" class="footer-social">TT</a>
            </div>
        </div>
        <div>
            <div class="footer-col-title">Mua Sắm</div>
            <ul class="footer-links">
                <li><a href="/Shop">Bộ sưu tập mới</a></li>
                <li><a href="/Shop">Áo Haori &amp; Kimono</a></li>
                <li><a href="/Shop">Quần Wide-leg</a></li>
                <li><a href="/Shop">Phụ kiện</a></li>
                <li><a href="/Shop">Sale</a></li>
            </ul>
        </div>
        <div>
            <div class="footer-col-title">Hỗ Trợ</div>
            <ul class="footer-links">
                <li><a href="@Url.Action("SizeGuide", "Home")">Hướng dẫn chọn size</a></li>
                <li><a href="@Url.Action("ShippingPolicy", "Home")">Chính sách vận chuyển</a></li>
                <li><a href="@Url.Action("ReturnPolicy", "Home")">Chính sách đổi trả</a></li>
                <li><a href="@Url.Action("Faq", "Home")">Câu hỏi thường gặp</a></li>
            </ul>
        </div>
        <div>
            <div class="footer-col-title">Liên Hệ</div>
            <ul class="footer-links">
                <li><a href="@Url.Action("Contact", "Home")">123 Đường Tố Hữu, Q.1</a></li>
                <li><a href="@Url.Action("Contact", "Home")">TP. Hồ Chí Minh</a></li>
                <li><a href="@Url.Action("Contact", "Home")">+84 123 456 789</a></li>
                <li><a href="@Url.Action("Contact", "Home")">hello@kizunaclothing.vn</a></li>
            </ul>
        </div>
    </div>
    <div class="footer-bottom">
        <span class="footer-copy">© 2025 Kizuna Clothing. All rights reserved.</span>
        <div class="footer-legal">
            <a href="@Url.Action("TermsOfService", "Home")">Điều khoản dịch vụ</a>
            <a href="@Url.Action("PrivacyPolicy", "Home")">Chính sách bảo mật</a>
        </div>
    </div>
</footer>
